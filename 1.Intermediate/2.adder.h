#ifndef ADDER_H
#define ADDER_H

// Function definitions can also be written directly in a header file.
// This is fine for small learning examples.

int addNumbers(int a, int b) {
    return a + b;
}

float addNumbers(float a, float b) {
    return a + b;
}

#endif